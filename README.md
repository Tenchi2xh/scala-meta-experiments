# Experimenting with `scala.meta`

SBT configuration based on https://github.com/iTakeshi/test_new_macro.scala

The example creates a case class keyword from scratch, called `@esac`

## Usage

Run the example:

```
$ sbt core/run
```
